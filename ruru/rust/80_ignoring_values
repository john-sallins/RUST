// Ignoring Remaining Parts of a Value with ..

// use specific parts and ignore the rest.

fn main()
{
struct Point {
    x: i32,
    y: i32,
    z: i32,
}



let origin = Point { x: 10, y: 0, z: 0 };

match origin {
    Point { x, .. } => println!("x is {}", x),
}
}